# Database connection
# For Docker: Use the credentials from docker-compose.yml
DATABASE_URL=postgresql://bookbuddy:bookbuddy_dev_password@localhost:5432/bookbuddy_dev
# For local PostgreSQL: Update with your credentials
# DATABASE_URL=postgresql://your_user:your_password@localhost:5432/bookbuddy_dev

# Server configuration
PORT=3001
NODE_ENV=development

# Session configuration
SESSION_SECRET=change-this-in-production-use-crypto-random-string

# Logging
LOG_LEVEL=info

# Rate limiting (FR-019)
RATE_LIMIT_MAX=100
